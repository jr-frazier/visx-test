import React from 'react';
import { geoEqualEarth, geoPath } from 'd3';

interface Props {
    
}
export default function Marks() {


    return (
        <g>  
            {data.features.map((d) => {
                return (
                    <path d={} />
                )
            })}
        </g>
    );
}
